# ── HTTP / scraping ──
requests>=2.31.0
beautifulsoup4>=4.12.0
urllib3>=2.0.0
curl_cffi>=0.15.0
trafilatura>=1.8.0
markdownify>=0.11.0

# ── CLI / TUI ──
rich>=13.7.0
prompt_toolkit>=3.0.0

# ── LLM ──
openai>=1.0.0
tiktoken>=0.6.0
tenacity>=8.2.0

# ── Filesystem / config ──
filelock>=3.13.0
python-dotenv>=1.0.0

# ── Testing (cap < 9.0: pytest 9 changed collector API, breaks us) ──
pytest>=7.4.0,<9.0
